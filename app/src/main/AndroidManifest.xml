<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="hitrash.jumismo.android.uoc.edu.hitrash">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".PrincipalUserActivity" />
        <activity android:name=".PrincipalAdminActivity" />
        <activity android:name=".UserListActivity" />
        <activity android:name=".ManagerHikingTrailActivity" />
        <activity android:name=".CleaningClaimsActivity" />
        <activity android:name=".HikingGroupActivity" />
        <activity android:name=".ConversationActivity" />
        <activity android:name=".CleaningGroupActivity" />
        <activity android:name=".HikingTrailsActivity" />
        <activity android:name=".NewHikingTrailActivity" />
        <activity android:name=".ShowHikingTrailActivity" />
        <activity android:name=".NewGroupActivity" />
        <activity android:name=".ProfileActivity"></activity>
    </application>

</manifest>